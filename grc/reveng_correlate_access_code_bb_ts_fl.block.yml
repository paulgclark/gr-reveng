# auto-generated by grc.converter

id: reveng_correlate_access_code_xx_ts_fl
label: Correlate AC TS - Fixed Len
category: Reverse Engineering

parameters:
-   id: access_code
    label: Access Code
    dtype: string
    default: '101010'
-   id: threshold
    label: Threshold
    dtype: int
    default: '0'
-   id: tagname
    label: Tag Name
    dtype: string
    default: packet_len
-   id: len
    label: Packet Length
    dtype: int
    default: '8'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: |-
        from gnuradio import digital
        import reveng
    make: |-
        reveng.correlate_access_code_bb_ts_fl(${access_code},
          ${threshold}, ${tagname}, ${len})

file_format: 1
