# auto-generated by grc.converter

id: reveng_manchester_decode
label: Manchester Decode PDU
category: '[Reverse Engineering]'

parameters:
-   id: invert
    label: Manchester Format
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    option_labels: [Non-Inverted, Inverted]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import reveng
    make: reveng.manchester_decode(${invert})

file_format: 1
