# auto-generated by grc.converter

id: reveng_pwm_decode
label: PWM Decoder
category: '[Reverse Engineering]'

parameters:
-   id: zero_seq
    label: Zero Sequence
    dtype: ''
    default: (0, 0, 1)
-   id: one_seq
    label: One Sequence
    dtype: ''
    default: (0, 1, 1)

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import reveng
    make: reveng.pwm_decode(${zero_seq}, ${one_seq})

file_format: 1
